// import { Campaign } from "../../controllers/campagins/Campaigns";

// const KeysToSkip = ['subDomains', 'terms', 'isFeatured', 'isDeepLinkEnabled']

// export function checkForUpdates(newCampaigns: Campaign[], oldCampaigns: Campaign[]){
//     let result = [];

//     newCampaigns.map((newCampaign) => {
//         oldCampaigns.map((oldCampaign) => {
//             if (newCampaign.campaignID === oldCampaign.campaignID) {

//             }
//         })
//     })  
// }